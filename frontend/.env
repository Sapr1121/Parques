# ============================================
# CONFIGURACIÓN PARA RED LOCAL (LAN)
# ============================================

# URL del Backend Node.js (API REST) - Puerto 3001
# Usa la IP de la máquina que corre el servidor
VITE_API_URL=http://localhost:3001

# URL del servidor WebSocket de juego (Python) - Puerto 8001
VITE_WS_URL=ws://localhost:8001

# URL del servidor Registry (Python) - Puerto 9000
VITE_REGISTRY_URL=http://localhost:9000
